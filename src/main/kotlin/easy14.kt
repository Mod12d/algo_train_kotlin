fun main() {
    var (N, W) = readLine()!!.split(" ").map { it.toInt() }
    var A = readLine()!!.split(" ").map { it.toInt() }.toMutableList()
    var cnt = 0

    //2個選択
    for (n in 0..N - 1) {

    }

}